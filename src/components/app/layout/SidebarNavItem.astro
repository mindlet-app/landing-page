---
const { icon: Icon, label, isActive = false } = Astro.props;
---

<li>
  <a
    class={`flex items-center gap-3 rounded-md px-3 py-2 text-sm font-medium text-slate-700 transition-colors ${
      isActive ? "bg-slate-100" : "hover:bg-slate-100"
    }`}
  >
    <Icon className={`h-5 w-5 ${isActive ? "text-blue-600" : "text-slate-500"}`} />
    {label}
  </a>
</li>