---
import { Image } from "astro:assets";
import hero from "../../assets/hero.svg";
import heroMobile from "../../assets/hero-mobile.svg";
---

<section
  class="min-h-screen flex flex-col justify-center items-center relative overflow-hidden px-4 md:px-0"
>
  <div class="absolute inset-0 w-full h-full p-4 md:p-24 animate-fade-in-scale">
    <Image
      src={heroMobile}
      alt="Background graph"
      class="w-full h-full object-contain object-center opacity-80 block sm:hidden"
    />
    <Image
      src={hero}
      alt="Background graph"
      class="w-full h-full object-contain object-center opacity-80 hidden sm:block"
    />
  </div>
  <div class="relative z-10 mt-16 md:mt-0 drop-shadow">
    <h1
      class="mb-2 font-saeada text-center text-3xl sm:text-4xl md:text-5xl lg:text-7xl animate-fade-in-up text-shadow-sm"
    >
      L&apos;apprentissage<br class="md:hidden" /> collaboratif,<br />
      assisté par l'IA
    </h1>

    <p
      class="relative z-10 font-light text-center text-base sm:text-lg md:text-xl xl:text-2xl animate-fade-in-up animation-delay-300 px-4 py-2 drop-shadow-2xl"
    >
      Le premier <span class="text-emerald-500 font-semibold"
        >réseau social</span
      >
      où apprendre rime avec partager.
    </p>
  </div>
</section>

<style>
  @keyframes fadeInScale {
    0% {
      opacity: 0;
      transform: scale(0.8);
    }
    100% {
      opacity: 1;
      transform: scale(1);
    }
  }

  @keyframes fadeInUp {
    0% {
      opacity: 0;
      transform: translateY(20px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-scale {
    animation: fadeInScale 1.2s ease-out forwards;
    opacity: 0;
  }

  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
    opacity: 0;
  }

  .animation-delay-300 {
    animation-delay: 300ms;
  }

  .text-shadow-sm {
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
  }
</style>
