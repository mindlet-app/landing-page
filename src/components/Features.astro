---
import { Image } from "astro:assets";
import Title from "./ui/Title";
import {
  LucideArrowRight,
  LucideBrain,
  LucideMedal,
  LucideMessagesSquare,
  LucideTrophy,
  LucideUsers,
} from "lucide-react";
import Paragraph from "./ui/Paragraph";
import BigTitle from "./ui/BigTitle";
---

<section
  id="fonctionnalites"
  class="py-16 md:py-24 px-4 md:px-10 bg-gradient-to-b from-neutral-50 to-emerald-50"
>
  <div class="container mx-auto max-w-6xl">
    <div class="text-center mb-12 md:mb-16">
      <BigTitle> Apprenez mieux, ensemble </BigTitle>
      <Paragraph className="max-w-2xl mx-auto">
        Mindlet combine les méthodes d'apprentissage les plus efficaces avec la
        puissance du social pour une expérience éducative révolutionnaire.
      </Paragraph>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 md:gap-8">
      <!-- Réseau social avec messagerie -->
      <div class="flex flex-col rounded-xl p-6">
        <div class="bg-emerald-100 text-emerald-600 p-3 rounded-lg w-fit mb-4">
          <LucideMessagesSquare className="w-6 h-6" />
        </div>
        <h3 class="text-xl font-semibold mb-3">Réseau social éducatif</h3>
        <p class="text-zinc-600 mb-4 flex-grow">
          Connectez-vous avec d'autres apprenants, partagez vos connaissances et
          échangez via notre messagerie intégrée. Trouvez des partenaires
          d'étude partout dans le monde.
        </p>
        <div class="mt-auto pt-4 border-t border-zinc-100">
          <div class="flex items-center gap-2">
            <div class="flex -space-x-2">
              <div
                class="w-8 h-8 rounded-full bg-emerald-200 flex items-center justify-center text-xs font-medium"
              >
                JD
              </div>
              <div
                class="w-8 h-8 rounded-full bg-blue-200 flex items-center justify-center text-xs font-medium"
              >
                ML
              </div>
              <div
                class="w-8 h-8 rounded-full bg-amber-200 flex items-center justify-center text-xs font-medium"
              >
                AR
              </div>
            </div>
            <span class="text-sm text-zinc-500">+2.4k utilisateurs actifs</span>
          </div>
        </div>
      </div>

      <!-- Groupes de collaboration -->
      <div
        class="bg-white rounded-xl shadow-sm hover:shadow-md transition-shadow p-6 border border-emerald-100 flex flex-col"
      >
        <div class="bg-blue-100 text-blue-600 p-3 rounded-lg w-fit mb-4">
          <LucideUsers className="w-6 h-6" />
        </div>
        <h3 class="text-xl font-semibold mb-3">Groupes de collaboration</h3>
        <p class="text-zinc-600 mb-4 flex-grow">
          Créez ou rejoignez des groupes thématiques pour partager des
          collections de cartes. Collaborez en temps réel pour enrichir vos
          connaissances et bénéficiez de l'intelligence collective.
        </p>
        <div class="mt-auto pt-4 border-t border-zinc-100">
          <div class="flex items-center justify-between">
            <span class="text-sm text-zinc-500">Accès à +500 groupes</span>
            <span
              class="text-xs bg-emerald-100 text-emerald-700 px-2 py-1 rounded-full"
              >Populaire</span
            >
          </div>
        </div>
      </div>

      <!-- Défis entre amis -->
      <div
        class="bg-white rounded-xl shadow-sm hover:shadow-md transition-shadow p-6 border border-emerald-100 flex flex-col"
      >
        <div class="bg-amber-100 text-amber-600 p-3 rounded-lg w-fit mb-4">
          <LucideTrophy className="w-6 h-6" />
        </div>
        <h3 class="text-xl font-semibold mb-3">Défis et tableaux de score</h3>
        <p class="text-zinc-600 mb-4 flex-grow">
          Lancez des défis à vos amis, suivez vos progrès sur les tableaux de
          classement et gagnez des badges. Transformez l'apprentissage en une
          expérience ludique et motivante.
        </p>
        <div class="mt-auto pt-4 border-t border-zinc-100">
          <div class="flex items-center gap-3">
            <div class="flex gap-1">
              <div
                class="w-5 h-5 rounded-full bg-amber-400 flex items-center justify-center text-white text-xs"
              >
                <LucideMedal className="w-3 h-3" />
              </div>
              <div
                class="w-5 h-5 rounded-full bg-zinc-300 flex items-center justify-center text-white text-xs"
              >
                <LucideMedal className="w-3 h-3" />
              </div>
              <div
                class="w-5 h-5 rounded-full bg-amber-700 flex items-center justify-center text-white text-xs"
              >
                <LucideMedal className="w-3 h-3" />
              </div>
            </div>
            <span class="text-sm text-zinc-500">Débloquez des récompenses</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Fonctionnalité mise en avant -->
    <div
      class="mt-12 md:mt-16 bg-gradient-to-r from-emerald-500 to-emerald-600 rounded-xl shadow-md overflow-hidden"
    >
      <div class="grid grid-cols-1 md:grid-cols-2">
        <div class="p-6 md:p-8 flex flex-col justify-center">
          <span class="text-emerald-100 text-sm font-medium mb-2">NOUVEAU</span>
          <h3 class="text-white text-2xl md:text-3xl font-semibold mb-4">
            IA collaborative
          </h3>
          <p class="text-emerald-50 mb-6">
            Notre intelligence artificielle analyse les performances de tous les
            utilisateurs pour optimiser les parcours d'apprentissage et suggérer
            les meilleures ressources pour chaque sujet.
          </p>
          <a
            href="#en-savoir-plus"
            class="inline-flex items-center gap-2 text-white hover:text-emerald-100 transition-colors w-fit"
          >
            En savoir plus
            <LucideArrowRight className="w-4 h-4" />
          </a>
        </div>
        <div class="bg-emerald-400 p-6 flex items-center justify-center">
          <div class="relative w-full max-w-xs aspect-square">
            <div
              class="absolute inset-0 bg-white/10 backdrop-blur-sm rounded-full animate-pulse-slow"
            >
            </div>
            <div
              class="absolute inset-4 bg-white/20 backdrop-blur-sm rounded-full animate-pulse-slow animation-delay-500"
            >
            </div>
            <div
              class="absolute inset-8 bg-white/30 backdrop-blur-sm rounded-full animate-pulse-slow animation-delay-1000"
            >
            </div>
            <div class="absolute inset-0 flex items-center justify-center">
              <LucideBrain className="w-16 h-16 text-white" />
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes pulseSlow {
    0%,
    100% {
      opacity: 1;
      transform: scale(1);
    }
    50% {
      opacity: 0.7;
      transform: scale(1.05);
    }
  }

  .animate-pulse-slow {
    animation: pulseSlow 4s ease-in-out infinite;
  }

  .animation-delay-500 {
    animation-delay: 500ms;
  }

  .animation-delay-1000 {
    animation-delay: 1000ms;
  }
</style>
